# Core dependencies
pillow
# Optional: required only for MP4 background playback.
# Install with `pip install opencv-python`.

# Platform-specific dependencies
pyudev ; sys_platform == "linux"
wmi ; sys_platform == "win32"
pywin32 ; sys_platform == "win32"
